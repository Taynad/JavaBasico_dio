public interface MusicPlayer {
    //na interface as propriedades são public, static e final
    //as variáveis já devem ser incializadas na interface
    //interface não tem construtor

    void playMusic();

    void pauseMusic();

    void stopMusic();
}
